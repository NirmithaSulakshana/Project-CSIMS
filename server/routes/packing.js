// server/routes/packing.js

const express = require("express");
const router = express.Router();
//const packingController = require("../controllers/packingController");

// Receive Orders Endpoint
//router.post("/orders", packingController.receiveOrder);

// Generate Packing List Endpoint
//router.get("/packing-list", packingController.generatePackingList);

module.exports = router;
